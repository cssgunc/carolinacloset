<!DOCTYPE html>
<html lang="en">

<head>
  <%- include("../components/baselinks") -%>
</head>

<body>
  <%- include("../components/header") -%>
  <div class="container">
    <%- include("../components/alerts") -%>
    <h1>Import Items</h1>
    <div class="row">
      <div class="col-sm-12">
        <div class="alert alert-secondary">
          <p>Expected format examples:</p>
          <ul>
            <li>(id), name, type, gender, image, brand, color, count, (size), (waist), (length), (chest), (sleeve)</li>
          </ul>
          <p>These fields are expected in this order</p>
          <p>The id field is required if you're importing a file exported from this program.</p>
          <p>The size, waist, length, chest, and sleeve fields depend on the type of item, but are expected in this order</p>
        </div>
      </div>
    </div>
    <div class="row">
      <div class="col-sm-12">
        <form method="POST" encType="multipart/form-data">
          <label for="file">Upload a CSV file</label>
          <div class="form-group">
            <input class="form-check-input" type="checkbox" id="fromExport" name='fromExport'>
            <label class="form-check-label" for="fromExport">File was exported from this program</label>
          </div>
          <div class="form-group">
            <input class="form-check-input" type="checkbox" id="withHeaders" name='withHeaders'>
            <label class="form-check-label" for="withHeaders">File has column headers in the first row</label>
          </div>
          <div class="form-group">
            <input type="file" class="form-control-file" id="file" name="file">
          </div>
          <button type="submit" class="btn btn-primary">Submit</button>
        </form>
      </div>
    </div>
    <%- include("../components/scripts") -%>
</body>

</html>